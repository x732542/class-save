jQuery JavaScript 並行
-> jQuery 能做到 JS也行

$.each()
執行$物件裡有一個each方法
執行[.] 前物件的each方法
$().each()
執行$方法，$方法回傳物件，執行$方法回傳物件的方法

*jQuery 0528(四)
    text(): 取得/設定 開始標籤結束標籤中間的內容
    html(): 取得/設定 開始標籤結束標籤中間的內容(有html標籤也會獲得)
    
    prop(): 取得使用者當前輸入的值
    attr(): 取得一開始寫在標籤屬性的值

    val():  取得/設定 使用者當前輸入的值

    before()  : 在指定元素前面插入

    after()   : 在指定元素後面插入

    prepend() : 插入在元素內部排列最前端

    append()  : 插入在元素內部排列最後端
    
    appendTo(): 插入在元素內部排列最後端(跟append差不多 但行動方式不同)

    empty()   : 清空 開始標籤結束標籤中間的內容
    
    remove()  : 移除 元素

    detach()  : 移除元素;如果元素使用jQuery的事件綁定，移除後加回事件還在。
   
    on()      : 綁定事件

    filter()  : 先找到集合，然後在 自己身上下 第二次的 條件(選擇)
    
    find()    : 先找到集合，然後在 自己內層  下第二次的 條件(選擇)
    
    children(): 子類選擇器 能選擇內層的所有內容 但無法再深入

    parent()  : 父類選擇器 能選外層內容 無法再延伸

    closest() : 父類選擇器 可向外部延伸

    css()     : 取得/設定 CSS樣式值

    $().each(): 先找到集合，然後指定要做的事

    $.each()  : 取得陣列的值 / 要做的事情

    this     : 關鍵字代表甚麼 -> 取決於你在哪裡使用他





















